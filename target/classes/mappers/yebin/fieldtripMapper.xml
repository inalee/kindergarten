<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yebin.mapper.fieldtripMapper">

<insert id="insertCategory">
 insert into category (catemain, catesub) values (#{catemain}, #{catesub});
</insert>

<insert id="insertCandidates" statementType="CALLABLE" parameterType="map">
{call fc_insertCandidates(#{cateVO.catemain}, #{cateVO.catesub}, #{areaVO.ararea}, #{areaVO.arcity}, #{fieldVO.ftperson},
 #{memVO.memid}, #{fieldVO.fttitle}, #{fieldVO.ftstart}, #{fieldVO.ftend}, #{fieldVO.ftweb}, #{fieldVO.ftaddr})};
</insert>

<!-- <select id="seletFtcodeforToken" parameterType="map"> -->
<!-- select * from fieldtrip where tecode = 2 and fttitle = '[대전] 2018 제11회 주니어닥터' order by ftdate desc limit 1; -->
<!-- </select> -->

<update id="updateFieldtrip">
 
</update>
</mapper>